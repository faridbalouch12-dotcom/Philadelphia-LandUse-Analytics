name: philly_dw
version: '0.1.0'
config-version: 2

profile: philly_dw

require-dbt-version: '>=1.11.0'

model-paths: ["models"]
test-paths: ["data-tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
